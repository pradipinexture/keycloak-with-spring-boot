server.port=1111

#Keycloak configuration
keycloak.realm = master
#keycloak.auth-server-url = http://localhost:8080/auth # It gives error while fetching data from keycloak
keycloak.auth-server-url = http://localhost:8080
keycloak.resource = realm-management-system
keycloak.public-client = true

# configure database details
spring.datasource.name=keycloak_realm
spring.datasource.url=jdbc:mysql:// localhost:3306/keycloak_realm
spring.datasource.username=root
spring.datasource.password=99097@Pradip
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update